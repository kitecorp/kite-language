// Top of diamond - imports both left and right, which both import common
import * from "imports/diamond_left.kite"
import * from "imports/diamond_right.kite"

var COMBINED = LEFT_VALUE + RIGHT_VALUE

fun process(number x) number {
    return leftProcess(x) + rightProcess(x)
}